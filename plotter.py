from jobnet import Jobnet


class Plotter:
    @staticmethod
    def plot(
        jobnets: dict[str, dict[str, Jobnet]],
        schedule: dict[str, list[Jobnet]],
        output: str,
        range: dict[str, str],
    ):
        pass
